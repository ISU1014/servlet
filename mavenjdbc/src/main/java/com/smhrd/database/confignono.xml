<!-- <?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
   PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
DB 연결 정보가 담겨있는 파일을 연결
	db.properties 파일에서는 띄어쓰기 X, 주석정리 X
	driver, url, username, password
<properties resources="com/smhrd/database/db.properties"/>


  <environments default="development">
    <environment id="development">
      <transactionManager type="JDBC"/>
      <dataSource type="POOLED">
        <property name="driver" value="${driver}"/>
        <property name="url" value="${url}"/>
        <property name="username" value="${username}"/>
        <property name="password" value="${password}"/>
      </dataSource>
    </environment>
  </environments>
  <mappers>
  SQL문을 관리할 mapper정리되어 있는 곳
  org/mybatis/example/BlogMapper.xml
    <mapper resource="com/smhrd/database/Mapper.xml"/>
  </mappers>
</configuration> -->